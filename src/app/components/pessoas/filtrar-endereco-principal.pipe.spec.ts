import { FiltrarEnderecoPrincipalPipe } from './filtrar-endereco-principal.pipe';

describe('FiltrarEnderecoPrincipalPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarEnderecoPrincipalPipe();
    expect(pipe).toBeTruthy();
  });
});
